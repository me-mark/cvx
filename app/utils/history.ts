import { createHashHistory } from 'history';
// const history = createBrowserHistory();
const history = createHashHistory(); // Use hashHistory towards to staticly running in localhost
export default history;
